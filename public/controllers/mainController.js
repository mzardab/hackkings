var app = angular.module('hackkings', []);

app.controller('MainController', function($scope, $http) {

    // insert code here

    // Function example (keeping data within scope)
    // $scope.getData = function(something){
        
    // };

});